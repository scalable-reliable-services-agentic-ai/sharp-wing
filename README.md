Prerequirements:

- docker
- [optionaly] uv


To run this toy example paste in terminal one of those commands:

```bash
docker compose up --build
```

```bash
make setup
```

Then open:
`http://localhost:5000/`
and
`http://localhost:5003/`
