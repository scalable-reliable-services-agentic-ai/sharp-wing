apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seeder.fullname" . }}-test-connection"
  labels:
    {{- include "seeder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seeder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
