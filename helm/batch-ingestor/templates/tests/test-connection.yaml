apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "batch-ingestor.fullname" . }}-test-connection"
  labels:
    {{- include "batch-ingestor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "batch-ingestor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
