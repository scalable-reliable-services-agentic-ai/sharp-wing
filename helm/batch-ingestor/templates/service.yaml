apiVersion: v1
kind: Service
metadata:
  name: {{ include "batch-ingestor.fullname" . }}
  labels:
    {{- include "batch-ingestor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "batch-ingestor.selectorLabels" . | nindent 4 }}
