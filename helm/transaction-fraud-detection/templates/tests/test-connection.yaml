apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transaction-fraud-detection.fullname" . }}-test-connection"
  labels:
    {{- include "transaction-fraud-detection.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "transaction-fraud-detection.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
