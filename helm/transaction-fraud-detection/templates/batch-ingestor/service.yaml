apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-batch-ingestor
  labels:
    app: batch-ingestor
spec:
  selector:
    app: batch-ingestor 
  ports:
    - name: metrics
      port: 8004
      targetPort: 8004
