apiVersion: v1
kind: Service
metadata:
  name: reporter-service
spec:
  type: LoadBalancer 
  selector:
    app: reporter
  ports:
    - protocol: TCP
      port: 5005        # Browser port (localhost:5005)
      targetPort: 5005  # Internal port in kubernetes
