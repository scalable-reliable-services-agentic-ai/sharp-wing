apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-validator
  labels:
    app: validator
spec:
  selector:
    app: validator
  ports:
    - name: metrics
      port: 8001
      targetPort: 8001