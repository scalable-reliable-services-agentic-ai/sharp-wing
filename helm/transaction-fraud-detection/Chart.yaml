apiVersion: v2
name: transaction-fraud-detection
# description: A Helm chart for Kubernetes
description: A Helm chart for Kubernetes for the transaction fraud detection system

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"


# apiVersion: v2
# name: transaction-fraud-detection
# description: A Helm chart for Kubernetes for the transaction fraud detection system
# type: application
# version: 0.1.0
# appVersion: "1.0"
dependencies:
  - name: anomaly-detection
    version: "0.1.0"
    repository: "file://../anomaly-detection"
  - name: batch-ingestor
    version: "0.1.0"
    repository: "file://../batch-ingestor"
  - name: fraud-triage
    version: "0.1.0"
    repository: "file://../fraud-triage"
  - name: generator
    version: "0.1.0"
    repository: "file://../generator"
  - name: grafana
    version: "0.1.0"
    repository: "file://../grafana"
  - name: kafka
    version: "0.1.0"
    repository: "file://../kafka"
  # - name: mcp-server
  #   version: "0.1.0"
  #   repository: "file://../mcp-server"
  - name: prometheus
    version: "0.1.0"
    repository: "file://../prometheus"
  - name: reporter
    version: "0.1.0"
    repository: "file://../reporter"
  - name: seeder
    version: "0.1.0"
    repository: "file://../seeder"
  - name: timescaledb
    version: "0.1.0"
    repository: "file://../timescaledb"
  - name: redis
    version: "0.1.0"
    repository: "file://../redis"
  - name: validator
    version: "0.1.0"
    repository: "file://../validator"