apiVersion: v1
kind: Service
metadata:
  name: {{ include "fraud-triage.fullname" . }}
  labels:
    {{- include "fraud-triage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fraud-triage.selectorLabels" . | nindent 4 }}
