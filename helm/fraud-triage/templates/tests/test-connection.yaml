apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fraud-triage.fullname" . }}-test-connection"
  labels:
    {{- include "fraud-triage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fraud-triage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
