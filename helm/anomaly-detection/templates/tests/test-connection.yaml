apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "anomaly-detection.fullname" . }}-test-connection"
  labels:
    {{- include "anomaly-detection.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "anomaly-detection.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
