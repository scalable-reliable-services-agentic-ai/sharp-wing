apiVersion: v1
kind: Service
metadata:
  name: {{ include "generator.fullname" . }}
  labels:
    {{- include "generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "generator.selectorLabels" . | nindent 4 }}
